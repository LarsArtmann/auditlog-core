# Features

> Honest inventory of what this project does, by status. Generated by studying
> the actual code, not the marketing claims. Updated as features ship, change,
> or break.

## Status legend

| Status                 | Meaning                                                      |
| ---------------------- | ------------------------------------------------------------ |
| `FULLY_FUNCTIONAL`     | Works as intended, exercised by tests or daily use.          |
| `PARTIALLY_FUNCTIONAL` | Ships but has known gaps, edge-case bugs, or missing pieces. |
| `BROKEN`               | Present in code but not working / disabled / failing.        |
| `PLANNED`              | Designed or documented but **not yet implemented** in code.  |

> A feature earns `FULLY_FUNCTIONAL` only when you can point to the code that
> delivers it AND confirm it works. If you're unsure, it's `PARTIALLY_FUNCTIONAL`
> at best, never round up.

## File Export (root package `auditlogcore`)

| Feature                | Status             | Notes                                                         |
| ---------------------- | ------------------ | ------------------------------------------------------------- |
| Atomic file write      | `FULLY_FUNCTIONAL` | `WriteToFile` in `helpers.go:52`; temp file + bufio + rename  |
| No-clobber guard       | `FULLY_FUNCTIONAL` | `CheckNoClobber` in `helpers.go:32`; returns `ErrFileExists`  |
| Sentinel errors        | `FULLY_FUNCTIONAL` | `ErrExportWriteFailed`, `ErrFileExists` in `helpers.go:16-22` |
| Buffered writes (64KB) | `FULLY_FUNCTIONAL` | `bufio.Writer` with 64KB buffer in `WriteToFile`              |

## Live Dashboard (package `live`)

| Feature                    | Status             | Notes                                                             |
| -------------------------- | ------------------ | ----------------------------------------------------------------- |
| SSE hub (fan-out)          | `FULLY_FUNCTIONAL` | `Hub` in `live/hub.go:42`; concurrent-safe, non-blocking send     |
| Subscriber management      | `FULLY_FUNCTIONAL` | `Subscribe`/`Unsubscribe` in `live/hub.go:78-108`                 |
| Lifecycle signaling        | `FULLY_FUNCTIONAL` | `SignalComplete` in `live/hub.go:115`; closes subscriber channels |
| HTTP dashboard server      | `FULLY_FUNCTIONAL` | `Server` in `live/server.go:69`; implements `http.Handler`        |
| Dashboard HTML endpoint    | `FULLY_FUNCTIONAL` | `GET {prefix}/` serves cached HTML from `DashboardProvider`       |
| Report JSON endpoint       | `FULLY_FUNCTIONAL` | `GET {prefix}/api/report` via `ReportProvider`                    |
| SSE events endpoint        | `FULLY_FUNCTIONAL` | `GET {prefix}/api/events`; snapshot + live events + completion    |
| Health check endpoint      | `FULLY_FUNCTIONAL` | `GET {prefix}/api/health`; uptime, clients, events, dropped       |
| Configurable URL prefix    | `FULLY_FUNCTIONAL` | `Config.Prefix` in `live/server.go:30`; routes under `{prefix}/`  |
| Heartbeat keepalive        | `FULLY_FUNCTIONAL` | `Config.HeartbeatInterval` in `live/server.go:38`; default 15s    |
| Functional options pattern | `FULLY_FUNCTIONAL` | `ServerOption` funcs in `live/server.go:123-151`                  |
| Graceful shutdown          | `FULLY_FUNCTIONAL` | `Server.Shutdown(ctx)` in `live/server.go:209`                    |
| Configurable read timeout  | `FULLY_FUNCTIONAL` | `Config.ReadHeaderTimeout`; default 5s                            |

## Test Coverage

| Area                  | Status             | Notes                                                   |
| --------------------- | ------------------ | ------------------------------------------------------- |
| WriteToFile (root)    | `FULLY_FUNCTIONAL` | 3 tests: success, atomic rename, write error            |
| CheckNoClobber (root) | `FULLY_FUNCTIONAL` | 2 tests: exists, not-exists                             |
| Hub (live)            | `FULLY_FUNCTIONAL` | 6 tests: subscribe/unsubscribe, broadcast, concurrent   |
| Server HTTP endpoints | `FULLY_FUNCTIONAL` | 7 tests: dashboard, health, report, 404, prefix, client |
| SSE streaming         | `FULLY_FUNCTIONAL` | 4 tests: snapshot, live event, complete, fan-out        |
| Server lifecycle      | `FULLY_FUNCTIONAL` | 1 test: graceful shutdown                               |
